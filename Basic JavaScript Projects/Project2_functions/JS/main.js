function myfunction() {
  var str = "I am learning";
  str += " the bootcamp"
  document.getElementById("Concatenate").innerHTML = str;
} //put ConConcatenated string into str and search the id=Concatenate and return str.

function project2_function(num) {
  num += 3;
  num *=2;
  document.getElementById("operator").innerHTML = num;
}
